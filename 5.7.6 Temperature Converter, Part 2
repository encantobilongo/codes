def c_to_f(c):
    try:
        f = c * (9/5) + 32
    except ValueError:
        return 0
    return f

def f_to_c(f):
    try:
        c = (f - 32) * 5/9
    except ValueError:
        return 0
    return c

print(c_to_f(1.01))
print(f_to_c(2.23))
